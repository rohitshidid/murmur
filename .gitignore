# macOS build
.build/
*.app/
*.xcuserdatad
.DS_Store

# Where the release workflow stages the DMG before uploading it. The image itself is
# built outside this tree (see the Makefile); this is only the copy CI makes.
dist/

# Windows / .NET build
[Bb]in/
[Oo]bj/
*.user
.vs/
artifacts/

# Local ASR model weights. Large, redistributed under their own license, and every
# platform fetches them itself — see docs/PARAKEET-WINDOWS.md.
models/
*.onnx
*.nemo
